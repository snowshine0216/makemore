[project]
name = "makemore"
version = "0.0.0"
description = "One-file character-level language modeling playground (makemore)."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "torch",
  "tensorboard",
  "matplotlib>=3.10.8",
]

[tool.uv]
package = false

